import { Wordler } from 'wordler-core';
import { Solver, Recommendation } from '@solver/index';
export { Recommendation } from '@solver/index';

export const solver: Wordler<Recommendation> = Solver;
